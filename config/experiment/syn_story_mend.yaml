# @package _global_

task: syn_story_mend
dataset: syn_story_mend
cbase: 1.0
train_prefix: "4K"
# doc_dropout: 0.5 # w. prob 0.5 randomly drop 1 doc
# val_batch_size: 1 # this is bc the model is trained with bs=1